# Required one-time Apps Script update

1. Replace the current Apps Script Code.gs with `google-apps-script/Code.gs`.
2. Save.
3. Run `upgradeSheets` once. Existing rows remain in place. New columns are added where needed.
4. Deploy > Manage deployments > Edit > New version > Deploy.
5. The existing `/exec` URL remains unchanged.
